<h1>Venues:</h1>

<ul>
<% @venues.each do |venue| %>
<li><%= link_to venue.name, venue_path(venue) %></li>
<% end %>
</ul>

<%= form_tag(venues_path, method: :get) do %>
<p>Search venue by capacity: <%= text_field_tag(:search, params[:search]) %> </p>
<%= submit_tag "Search", name: nil %>
<% end %>