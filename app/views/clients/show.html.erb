<h1><%= "Hello, #{@client.first_name}!" %></h1>

Upcoming Events: 
<ul>
<% @client.upcoming_events.each do |event| %>
 <li> <%= link_to(event.title, event_path(event)) %></li>
  <% end %>
  </ul>
Past Events:
<ul>
<% @client.past_events.each do |event| %>
 <li> <%= link_to(event.title, event_path(event)) %></li>
  <% end %>
</ul>

<%= button_to('Edit Profile', edit_client_path(@client), method: :get, class:'btn submit')%>
<br>
<%= button_to('Logout', logout_path, method: :delete, class:'btn submit') %>